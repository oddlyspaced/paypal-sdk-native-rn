import { SafeAreaView, Text, View } from 'react-native';

const App = () => {
	return (
		<SafeAreaView
			style={{
				flex: 1,
			}}
		>
			<Text
				style={{
					flex: 1,
					textAlign: 'center',
				}}
			>
				PayPal JS Sdk Test
			</Text>
		</SafeAreaView>
	);
};

export default App;
